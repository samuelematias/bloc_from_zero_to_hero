name: bloc_from_zero_to_hero
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^6.1.2
  equatable: ^1.2.6
  connectivity_plus: ^0.8.1
  hydrated_bloc: ^6.1.0
  path_provider: ^1.6.27

  cupertino_icons: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^7.1.0

flutter:
  uses-material-design: true
